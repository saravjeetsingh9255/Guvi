step 1: creates my-nginx folder  and 3 files "Dockerfile" , "docker-compose.yml" , "index.html"

step2: build the image

step3: hit the command for docker compose up for run the container

step4: login to docker hub with credentials for pushing image

step5: tagged the image for pushing to docker hub

step6: pushed the tagged image to docker hub screen shot attached image visible on hub after pushing